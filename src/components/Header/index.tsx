import React from "react";
import "./styles.scss";
import Logo from "./components/Logo";

const Header = () => {
  return (
    <header id="blocks-front-header" className="blocks-front-header">
      <Logo />
    </header>
  );
};

export default Header;
